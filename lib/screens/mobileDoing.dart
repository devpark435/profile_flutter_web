import 'package:flutter/material.dart';

class MobileDoingScreen extends StatelessWidget {
  const MobileDoingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
